# 필마트 구현 설명서

## 1. 프로젝트 개요

### 1.1 목적
동네 식자재 마트의 온라인 주문 시스템을 구축하여, 고객이 모바일/웹으로 편리하게 주문하고 배달 또는 픽업으로 받을 수 있는 서비스를 제공합니다.

### 1.2 핵심 가치
- 동네 주민들의 편리한 장보기
- 전화 주문 부담 감소
- 배달/픽업 주문 관리 효율화

### 1.3 타겟 사용자
- **주부층**: 집에서 편하게 장보기
- **직장인**: 퇴근 후 픽업
- **시니어층**: 큰 글씨, 간단한 UI

---

## 2. 기술 스택

### 2.1 프론트엔드
| 기술 | 용도 |
|------|------|
| HTML5 | 페이지 구조 |
| CSS3 | 스타일링 (모바일 우선 반응형) |
| JavaScript (Vanilla) | 동적 기능 구현 |
| Google Fonts (Noto Sans KR) | 한글 폰트 |

### 2.2 백엔드
| 기술 | 용도 |
|------|------|
| Node.js | 런타임 환경 |
| Express.js | 웹 프레임워크 |
| JSON 파일 | 데이터 저장소 |
| bcryptjs | 비밀번호 암호화 |
| express-session | 세션 관리 |
| multer | 이미지 업로드 |

### 2.3 선택 이유
- **Vanilla JS**: 별도 빌드 과정 없이 바로 실행 가능
- **JSON 파일 저장**: SQLite 대신 사용하여 네이티브 모듈 컴파일 불필요
- **Express.js**: 가장 널리 사용되는 Node.js 웹 프레임워크

---

## 3. 프로젝트 구조

```
필마트/
├── server.js              # Express 서버 (API 엔드포인트)
├── package.json           # 프로젝트 설정 및 의존성
├── README.md              # 프로젝트 소개
├── 구현설명서.md           # 현재 문서
│
├── data/                  # 데이터 저장소 (자동 생성)
│   ├── categories.json    # 카테고리 데이터
│   ├── products.json      # 상품 데이터
│   ├── users.json         # 회원 데이터
│   └── orders.json        # 주문 데이터
│
├── public/                # 정적 파일 (클라이언트)
│   ├── index.html         # 고객용 메인 페이지
│   ├── admin.html         # 관리자 페이지
│   │
│   ├── css/
│   │   ├── style.css      # 고객용 스타일
│   │   └── admin.css      # 관리자용 스타일
│   │
│   ├── js/
│   │   ├── app.js         # 고객용 JavaScript
│   │   └── admin.js       # 관리자용 JavaScript
│   │
│   └── images/            # 상품 이미지 저장
│
└── docs/                  # 사용 설명서
    ├── 고객용_사용설명서.md
    └── 관리자_매뉴얼.md
```

---

## 4. 데이터베이스 설계

### 4.1 카테고리 (categories.json)
```javascript
{
    id: Number,           // 고유 ID
    name: String,         // 카테고리명 (예: "신선식품")
    icon: String,         // 이모지 아이콘 (예: "🥬")
    sort_order: Number    // 정렬 순서
}
```

### 4.2 상품 (products.json)
```javascript
{
    id: Number,           // 고유 ID
    name: String,         // 상품명
    price: Number,        // 가격 (원)
    category_id: Number,  // 카테고리 ID (FK)
    description: String,  // 상품 설명
    image_url: String,    // 이미지 경로
    stock: Number,        // 재고 수량
    is_active: Number,    // 판매 상태 (1: 판매중, 0: 비활성)
    created_at: String    // 등록일 (ISO 8601)
}
```

### 4.3 회원 (users.json)
```javascript
{
    id: Number,           // 고유 ID
    name: String,         // 이름
    phone: String,        // 전화번호 (고유)
    password: String,     // 비밀번호 (bcrypt 해시)
    address: String,      // 기본 주소
    created_at: String    // 가입일
}
```

### 4.4 주문 (orders.json)
```javascript
{
    id: Number,              // 고유 ID
    order_number: String,    // 주문번호 (YYYYMMDD-XXXX)
    user_id: Number|null,    // 회원 ID (비회원: null)
    user_name: String,       // 주문자명
    user_phone: String,      // 연락처
    address: String,         // 배송 주소
    delivery_type: String,   // "delivery" 또는 "pickup"
    delivery_request: String,// 배송 요청사항
    payment_method: String,  // "cash" 또는 "card"
    total_amount: Number,    // 상품 금액
    delivery_fee: Number,    // 배달비
    status: String,          // 주문 상태
    admin_memo: String,      // 관리자 메모
    items: Array,            // 주문 상품 목록
    created_at: String       // 주문일
}
```

### 4.5 주문 상태 흐름
```
[배달 주문]
pending(주문접수) → preparing(준비중) → delivering(배달중) → completed(완료)
                                                          ↘ cancelled(취소)

[픽업 주문]
pending(주문접수) → preparing(준비중) → pickup_ready(픽업대기) → pickup_completed(픽업완료)
                                                              ↘ cancelled(취소)
```

---

## 5. API 설계

### 5.1 인증 API

| 메서드 | 엔드포인트 | 설명 |
|--------|------------|------|
| POST | `/api/auth/register` | 회원가입 |
| POST | `/api/auth/login` | 로그인 |
| POST | `/api/auth/logout` | 로그아웃 |
| GET | `/api/auth/me` | 현재 로그인 사용자 정보 |
| PUT | `/api/auth/update` | 회원정보 수정 |

### 5.2 상품 API

| 메서드 | 엔드포인트 | 설명 |
|--------|------------|------|
| GET | `/api/categories` | 카테고리 목록 |
| GET | `/api/products` | 상품 목록 (필터: category, search) |
| GET | `/api/products/:id` | 상품 상세 |

### 5.3 주문 API

| 메서드 | 엔드포인트 | 설명 |
|--------|------------|------|
| POST | `/api/orders` | 주문 생성 |
| GET | `/api/orders` | 주문 내역 조회 (phone 파라미터) |
| GET | `/api/orders/:orderNumber` | 주문 상세 |

### 5.4 관리자 API

| 메서드 | 엔드포인트 | 설명 |
|--------|------------|------|
| POST | `/api/admin/login` | 관리자 로그인 |
| POST | `/api/admin/logout` | 관리자 로그아웃 |
| GET | `/api/admin/check` | 관리자 인증 확인 |
| GET | `/api/admin/stats` | 통계 조회 |
| GET | `/api/admin/products` | 상품 목록 (전체) |
| POST | `/api/admin/products` | 상품 등록 |
| PUT | `/api/admin/products/:id` | 상품 수정 |
| DELETE | `/api/admin/products/:id` | 상품 비활성화 |
| POST | `/api/admin/products/:id/toggle-stock` | 품절 토글 |
| GET | `/api/admin/orders` | 주문 목록 |
| PUT | `/api/admin/orders/:id/status` | 주문 상태 변경 |
| PUT | `/api/admin/orders/:id/memo` | 주문 메모 저장 |

---

## 6. 주요 기능 구현

### 6.1 고객 기능

#### 6.1.1 상품 목록/검색
```javascript
// 카테고리 필터
function filterCategory(categoryId) {
    loadProducts(categoryId);
}

// 상품명 검색
function searchProducts() {
    const query = document.getElementById('searchInput').value;
    loadProducts(null, query);
}
```

#### 6.1.2 장바구니
- LocalStorage에 장바구니 데이터 저장
- 페이지 새로고침해도 장바구니 유지
- 수량 변경, 삭제 기능

```javascript
// 장바구니 저장
function saveCart() {
    localStorage.setItem('pilmart_cart', JSON.stringify(cart));
}

// 장바구니 로드
let cart = JSON.parse(localStorage.getItem('pilmart_cart')) || [];
```

#### 6.1.3 주문 프로세스
1. 배송 정보 입력 (이름, 전화번호, 주소)
2. 배송 방법 선택 (배달/픽업)
3. 결제 방법 선택 (현금/카드 - 만나서 결제)
4. 주문 완료 → 주문번호 발급

```javascript
// 배달비 계산
let delivery_fee = 0;
if (delivery_type === 'delivery' && total_amount < 30000) {
    delivery_fee = 3000;  // 3만원 미만 시 배달비 3,000원
}
```

#### 6.1.4 주문 내역 조회
- 회원: 로그인 시 자동 조회
- 비회원: 전화번호 입력으로 조회

### 6.2 관리자 기능

#### 6.2.1 대시보드
- 오늘 주문 건수
- 오늘 매출
- 이번 주 매출
- 신규 주문 (미처리)

```javascript
// 통계 계산
const todayOrders = orders.filter(o =>
    o.created_at.slice(0, 10) === today
);
const todaySales = todayOrders
    .filter(o => o.status !== 'cancelled')
    .reduce((sum, o) => sum + o.total_amount + o.delivery_fee, 0);
```

#### 6.2.2 주문 관리
- 주문 목록 조회 (상태별, 날짜별 필터)
- 주문 상태 변경
- 관리자 메모 기능
- 고객 전화 바로 걸기

#### 6.2.3 상품 관리
- 상품 등록/수정
- 이미지 업로드
- 품절 처리 토글
- 상품 비활성화

#### 6.2.4 신규 주문 알림
- 30초마다 새 주문 체크
- 새 주문 시 알림음 재생
- 팝업 알림 표시

```javascript
// 30초마다 새 주문 확인
checkInterval = setInterval(checkNewOrders, 30000);

function checkNewOrders() {
    // 새 주문이 있으면 알림
    if (stats.newOrders > lastOrderCount) {
        playNotificationSound();
        showNotificationPopup(stats.newOrders - lastOrderCount);
    }
}
```

---

## 7. UI/UX 설계

### 7.1 디자인 시스템

#### 색상 팔레트
```css
:root {
    --primary: #4CAF50;      /* 메인 색상 (초록 - 신선함) */
    --primary-dark: #388E3C;
    --secondary: #FF9800;    /* 강조 색상 (주황) */
    --background: #f5f5f5;
    --white: #ffffff;
    --text: #333333;
    --text-light: #666666;
    --border: #e0e0e0;
    --danger: #f44336;
}
```

#### 폰트 크기 (시니어 고려)
```css
body {
    font-size: 16px;  /* 모바일 기본 */
}
.product-name { font-size: 16px; }
.product-price { font-size: 18px; }
.section-title { font-size: 20px; }
.page-title { font-size: 24px; }
```

### 7.2 모바일 우선 반응형

```css
/* 모바일 기본 (2열 그리드) */
.products-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 12px;
}

/* 태블릿 (3열) */
@media (min-width: 768px) {
    .products-grid {
        grid-template-columns: repeat(3, 1fr);
    }
}

/* 데스크톱 (4열) */
@media (min-width: 1024px) {
    .products-grid {
        grid-template-columns: repeat(4, 1fr);
    }
}
```

### 7.3 주요 UI 컴포넌트

#### 상품 카드
```
┌──────────────┐
│  [상품 이미지] │
│              │
│ 상품명        │
│ 5,000원      │
│ [담기 버튼]   │
└──────────────┘
```

#### 하단 네비게이션
```
┌─────────────────────────────────┐
│  🏠 홈  │  🛒 장바구니  │  📋 주문  │  👤 마이  │
└─────────────────────────────────┘
```

---

## 8. 보안 고려사항

### 8.1 비밀번호 암호화
```javascript
const bcrypt = require('bcryptjs');

// 회원가입 시 암호화
const hashedPassword = await bcrypt.hash(password, 10);

// 로그인 시 검증
const isValid = await bcrypt.compare(password, user.password);
```

### 8.2 세션 관리
```javascript
app.use(session({
    secret: 'pilmart-secret-key-2024',
    resave: false,
    saveUninitialized: false,
    cookie: { maxAge: 24 * 60 * 60 * 1000 }  // 24시간
}));
```

### 8.3 관리자 인증 미들웨어
```javascript
const adminAuth = (req, res, next) => {
    if (req.session.admin) {
        next();
    } else {
        res.status(401).json({
            success: false,
            message: '관리자 로그인이 필요합니다.'
        });
    }
};

// 사용 예시
app.get('/api/admin/orders', adminAuth, (req, res) => {
    // 관리자만 접근 가능
});
```

---

## 9. 운영 조건

| 항목 | 조건 |
|------|------|
| 배달 지역 | 마트 반경 2km 이내 |
| 영업 시간 | 오전 9시 ~ 오후 9시 |
| 최소 주문금액 | 10,000원 |
| 배달비 | 3,000원 (30,000원 이상 무료) |
| 결제 방법 | 만나서 결제 (현금/카드) |

---

## 10. 설치 및 실행

### 10.1 요구사항
- Node.js 18.x 이상
- npm 8.x 이상

### 10.2 설치
```bash
# 프로젝트 디렉토리로 이동
cd 필마트

# 패키지 설치
npm install
```

### 10.3 실행
```bash
# 서버 시작
npm start

# 또는 개발 모드 (자동 재시작)
npm run dev
```

### 10.4 접속
- 고객 페이지: http://localhost:3000
- 관리자 페이지: http://localhost:3000/admin.html

### 10.5 관리자 로그인 정보
- 아이디: `admin`
- 비밀번호: `pilmart2024`

---

## 11. 향후 개선 사항

### 11.1 2단계 (중요)
- [ ] SMS 알림 연동 (NHN Cloud, Twilio)
- [ ] 온라인 결제 연동 (토스페이먼츠)
- [ ] 카카오 로그인

### 11.2 3단계 (개선)
- [ ] PWA 지원 (오프라인 캐싱)
- [ ] 푸시 알림
- [ ] 재입고 알림
- [ ] 이번 주 전단 행사
- [ ] 상품 리뷰 기능

### 11.3 운영 개선
- [ ] 데이터베이스 마이그레이션 (PostgreSQL)
- [ ] 이미지 CDN 연동
- [ ] 로그 모니터링
- [ ] 자동 백업

---

## 12. 문의 및 지원

### 기술 지원
- 개발 관련 문의는 개발자에게 연락

### 운영 문의
- 전화: 02-123-4567
- 영업시간: 오전 9시 ~ 오후 9시

---

*이 문서는 필마트 온라인 주문 시스템의 구현 내용을 설명합니다.*
*최종 업데이트: 2024년 12월*
